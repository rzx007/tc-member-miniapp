// Copy this file to config.private.js and fill in the current AppID's OpenIDs.
// config.private.js is ignored by Git but is included in local cloud deployment.
module.exports = {
  employees: [
    // { openid: "CURRENT_APP_OPENID", name: "管理员", role: "super_admin" },
    // { openid: "CURRENT_APP_OPENID", name: "员工姓名", role: "operator" },
  ],
};
